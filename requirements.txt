# --- Core pipeline (필수) ---
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
matplotlib>=3.4.0

# --- Statistics (필수) ---
# pingouin: stats_analyzer.py (RM-ANOVA / ICC / effect size)
pingouin>=0.5.4

# --- Statistics (선택, 권장) ---
# statsmodels: stats_grf_eag.py의 혼합모형(mixed model) dose-response.
#   미설치 시 subject별 OLS 기울기 집단검정으로 자동 대체됨.
statsmodels>=0.14.0
