# 연구원 작업 안내 (offset / edge annotation)

브라우저로만 하는 작업입니다. 파이썬 설치, 데이터 복사, git **모두 불필요**합니다.

> **판정 기준은 이 문서에 없습니다.** "무엇을 옳다고 보는가"는 전부
> **[`ANNOTATION_PROTOCOL.md`](ANNOTATION_PROTOCOL.md)** 에 있습니다.
> 작업 전에 그 문서의 **§2 (측정 프로토콜)** 와 **§5 (조작적 정의)** 를 읽으세요. 10분이면 됩니다.
> 이 문서는 어디를 누르는지만 설명합니다.

---

## 0. 처음 한 번만

1. 브라우저에서 **`http://medibb.synology.me:18440/`** 접속 → 받은 비밀번호로 로그인
2. 로그인 상태가 유지되면 아래 GUI 주소가 바로 열립니다

> ⚠️ **비밀번호는 연구실 공용입니다.** 개인 계정이 아니라 모두가 같은 비밀번호를 쓰는
> 구조라, 로그인하면 서버 전체에 접근됩니다. **외부에 공유하지 마세요.**
> 필요한 것은 아래 두 GUI 주소뿐입니다. **다른 폴더는 열지도, 고치지도 마세요.**

| 작업 | 주소 (**끝 슬래시 `/` 필수**) |
|---|---|
| ① offset 맞추기 | `http://medibb.synology.me:18440/proxy/8766/` |
| ② edge 맞추기 | `http://medibb.synology.me:18440/proxy/8765/` |

> 신뢰도 파일럿에 배정받으셨다면 **주소가 다릅니다. [§8](#8-신뢰도-파일럿-해당자만)을
> 먼저 보세요.** 위 두 주소는 본 작업용입니다.

접속이 안 되면 앱이 꺼진 것입니다. 교수님께 요청하거나 code-server 터미널에서:

```bash
cd /workspace/research/EAGstatic
python3 offset_app.py --host 0.0.0.0 --port 8766   # 한 탭에서
python3 edge_app.py   --host 0.0.0.0 --port 8765   # 다른 탭에서
```

터미널을 닫으면 앱도 꺼집니다. 각각 별도 탭에 띄워둔 채로 두세요.

---

## 1. 30초 요약

한 세션에서 대상자는 **한발서기를 4회** 합니다. 회차마다 검사측 다리에 싣는 체중이
커집니다. 각 회차마다 **부하가 실리는 순간**과 **빠지는 순간**이 있으므로,
**세션·채널당 이벤트는 8개**입니다.

기억할 규칙 두 개:

1. **offset 먼저, edge 나중.** offset을 바꾸면 그 세션의 edge가 전부 어긋납니다
2. **부하가 실리면 EAG 하강(fall), 빠지면 상승(rise).** 예외 없습니다

---

## 2. Step 1. OFFSET (반드시 먼저)

`.../proxy/8766/`

1. 상단 드롭다운에서 세션 선택. 검토대상 `▲`이 위로 정렬됩니다.
   필터로 `전체 / 검토대상 / 확정 / 미확정` 전환
2. 아래 **CORRECTED 패널에서 GRF 사각파와 EAG가 잘 겹치면 그대로 두고 다음 세션으로**
   (손댈 것 없습니다. 자동값이 그대로 적용됩니다)
3. 안 맞으면 → **위 GRF 패널에서 기준점 클릭 → 아래 EAG 패널에서 대응점 클릭**.
   쌍이 성립하며 EAG가 겹치도록 이동합니다
4. **쌍을 2~3개 찍으세요.** 중앙값을 쓰므로 한 쌍이 부정확해도 견딥니다
5. 맞으면 **Save** → `✅ [확정 ...s]` 표시

**판정 기준은 [PROTOCOL §5.1](ANNOTATION_PROTOCOL.md#51-offset-정렬이-맞다).**
plateau가 아니라 **전이 지점**이 겹치는지를 봅니다. 일부만 맞고 일부가 어긋나면
정렬 문제가 아니라 cycle-skip을 의심하세요.

**조작**: 휠 = 확대/축소, 드래그 = 좌우 이동. 클릭 흡착은 `가장 가까운 점`(기본) ·
`변곡점` · `자유` 중 선택. 보조 기능으로 match-rate 프로파일 클릭, `best-match 값 사용`,
`nudge ±0.02/±0.1`, offset 직접 입력이 있습니다. **Clear manual**로 자동값 복귀.

---

## 3. Step 2. EDGE (offset 확정 후에만)

`.../proxy/8765/`

1. 드롭다운에서 세션 선택 + 채널(`ch`) 지정.
   상단에 **`cycle n/4 · 이벤트 m/8`** 검증이 표시됩니다
2. 화면 구성:
   - **주황 음영** = 체중부하 cycle 4회
   - **세로 점선** = anchor 8개 (빨강 = 부하 시작, 파랑 = 이탈)
   - **굵은 빨강 `누락`** = 그 자리에 edge가 없음 → 추가해야 함
   - **회색 점선** = 노이즈 후보 (anchor에서 1.2초 넘게 떨어진 것)
3. 수정:
   - knee 점 **드래그**로 이동
   - **Add mode** 후 2점 클릭으로 추가
   - 잘못된 것 선택 후 **Delete** 또는 Del 키
   - **snap** 체크 시 corner 자동 정렬
   - **노이즈 삭제** 버튼 = anchor에서 먼 edge 일괄 제거
4. **`✅ 프로토콜 충족`(8/8)** 이 되면 **Save**

**판정 기준은 [PROTOCOL §5.2](ANNOTATION_PROTOCOL.md#52-edge-onset--offset-knee)와
[§5.4](ANNOTATION_PROTOCOL.md#54-애매동점-상황).** 세 가지만 기억하세요:

- knee는 **극값(peak)이 아니라 꺾이는 지점(corner)** 입니다
- 표의 `후보` 라벨은 **지우지 마세요.** 경계 근처의 진짜 반응일 수 있습니다.
  구간 한가운데의 `노이즈`만 지웁니다
- 방향 규칙(부하 = 하강)에 어긋나면 잘못 잡힌 것입니다. 지우고 다시 잡으세요

**확대해서 훑는 것이 가장 빠릅니다.**

| 조작 | 동작 |
|---|---|
| 휠 | 커서 기준 확대/축소 |
| 빈 공간 드래그 | 좌우 이동 |
| ← → | 보이는 폭의 25%씩 이동 |
| **Shift + ← →** | **이전/다음 anchor로 점프** (배율 유지, 화면 중앙 정렬) |
| Home / End | 세션 처음 / 끝 |
| f | 전체보기 |

**8개 anchor를 `Shift + →`로 차례로 넘기며 확인하는 것이 표준 작업 방식입니다.**

---

## 4. Step 3. 못 쓸 데이터는 ⛔ 라벨만

아무리 봐도 못 맞추는 경우가 있습니다. 붙잡고 있지 말고 라벨만 붙이고 넘어가세요.

- **offset 화면**: `⛔ 세션 제외`
- **edge 화면**: `⛔ 채널 제외` (그 채널만) · `⛔ 세션 제외` (전 채널)
- **사유**를 고르고(`노이즈` · `동기화불가` · `프로토콜이상` · `기록오류` · `기타`),
  **메모를 한 줄 남겨주세요.** 나중에 왜 뺐는지 판단하는 근거가 됩니다
- 잘못 눌렀으면 같은 버튼이 `⛔ 제외 해제`로 바뀝니다

> **삭제가 아니라 라벨입니다.** 데이터는 그대로 남고 통계에서만 빠지므로 언제든
> 되돌릴 수 있습니다. **애매하면 제외하지 말고 그냥 두세요**
> (`⚠️ 검토 필요`로 남으면 교수님이 다시 봅니다).

---

## 5. 저장

**Save를 누르면 서버의 공유 `result/`에 즉시 기록됩니다.** 따로 보낼 것 없습니다.
(`manual_offsets.json` · `manual_edges.json` · `exclusions.json`)

> 신뢰도 파일럿(§8)은 `result/reliability/rater_A|B/` 아래에 따로 쌓입니다.
> 본 작업 결과와 섞이지 않으니 그대로 두시면 됩니다.

git 이력으로 남기려면 (선택):

```bash
cd /workspace/research/EAGstatic
git add result/                       # 확정값 JSON 3개만 스테이징됨
git commit -m "annotate: offset/edge 확정 (연구원)"
git pull --rebase && git push
```

대용량 데이터는 `.gitignore`로 자동 제외되어 JSON 3개만 들어갑니다.

---

## 6. 진행 현황

```bash
python3 run_pipeline.py status       # 전체 백로그 요약
python3 offset_review.py --list      # offset 확정된 세션
python3 edge_editor.py list          # edge 확정된 세션·채널
python3 edge_review.py --list        # edge 검토 대상 요약
```

---

## 7. 반드시 지킬 것

- **offset을 확정하기 전에 edge를 손대지 마세요.** edge는 보정된 시간축에 저장되어
  offset이 바뀌면 전부 어긋납니다
- **port 3002는 절대 사용·종료 금지** (api-server 전용).
  GUI는 본 작업 8765 / 8766, 신뢰도 파일럿 8768~8771(§8)만
- **여러 사람이 같은 세션을 동시에 저장하면 마지막 저장이 이깁니다. 세션을 나눠 맡으세요**
- **`/workspace` 아래 다른 프로젝트 폴더는 열지도, 고치지도 마세요**
- 터미널에서 `git` 명령은 **5장의 `result/` 커밋 외에는 쓰지 마세요.**
  브랜치 전환(`git checkout`)은 진행 중인 확정값을 되돌릴 수 있습니다

---

## 8. 신뢰도 파일럿 (해당자만)

허용 오차를 **두 사람의 실제 일치도에서 구하기 위한** 사전 작업입니다. 본 작업과
**주소도 저장 위치도 다릅니다.** 본 작업(8765/8766)과 헷갈리지 마세요.

| 담당 | ① offset | ② edge |
|---|---|---|
| **rater A** | `.../proxy/8768/` | `.../proxy/8769/` |
| **rater B** | `.../proxy/8770/` | `.../proxy/8771/` |

**본인 포트 두 개만 쓰세요.** 상대 포트를 열면 상대 저장소에 쓰게 되고, 그 순간
파일럿이 무효가 됩니다. 시작 전에 본인이 A인지 B인지 확인하세요.

세 가지가 본 작업과 다릅니다.

1. **자동값이 없습니다.** 화면에 EAG edge가 하나도 없고 offset도 보정 전 상태입니다.
   고장이 아니라 의도된 것입니다 — 둘 다 같은 자동값을 받으면 "둘 다 손대지 않음"이
   완전 일치로 잡혀, 재는 것이 사람의 일치도가 아니라 알고리즘의 결정론성이 됩니다
2. **GRF anchor(세로 점선) 8개는 그대로 보입니다.** 그 자리의 knee를 찾는 것이 일입니다
3. **상대가 무엇을 했는지 볼 수 없습니다.** 정상입니다. 상의하지 말고 각자 하세요

대상은 **10세션으로 고정**되어 있습니다(`result/reliability/pilot_sessions.csv`).
드롭다운에는 전체 세션이 보이지만 **목록에 있는 것만** 작업하세요.

판정 기준은 본 작업과 **완전히 동일**합니다 —
[PROTOCOL §5](ANNOTATION_PROTOCOL.md#5-조작적-정의). 애매하면 §5.4대로 등급 라벨을
남기고 넘어가세요. **평소보다 신중하게 하지 마세요.** 평소 그대로 해야 평소의
일치도가 측정됩니다.

---

## 9. 훈련과 자격

본 작업 투입 전에 거칩니다. 상세는
**[PROTOCOL §6](ANNOTATION_PROTOCOL.md#6-rater-자격과-훈련)**.

1. `ANNOTATION_PROTOCOL.md` §2, §5 읽기
2. 연습 **10세션** (교수님 주석과 대조하며 피드백)
3. 자격 판정 **5세션** 독립 수행 → **일치율 80% 이상**이면 본 작업 시작
4. 캠페인 종료 시 초기 5세션 재주석 (기준이 흔들리지 않았는지 확인)

---

## 문의

- 판정 기준: `ANNOTATION_PROTOCOL.md`
- 코드 구조·CLI: `README.md`
- 통계 설계: `STATS_PLAN.md`
